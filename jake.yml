---
source_directory:     source
build_directory:      build
layout:               together
builds:
  src:
    packer:           false
  min:
    shrink_vars:      true
    private:          true

packages:
  dynamicaudio:
    files:
      - swfobject
      - dynamicaudio
